package com.ticketmaster.eventpromoterapp.download;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication
public class EventPromoterAppApplication {
	//method to start the spring application
	//this method is required in each springboot application

	public static void main(String[] args) {
		SpringApplication.run(EventPromoterAppApplication.class, args);
	}

}
